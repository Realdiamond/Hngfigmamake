
  # Screen design request (Community)

  This is a code bundle for Screen design request (Community). The original project is available at https://www.figma.com/design/pE4lbRlznZExbd0q2zfE1O/Screen-design-request--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  